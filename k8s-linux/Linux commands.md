## Useful Linux commands

#### To check DNS resolution with public DNS
```
$ dig @8.8.8.8 <replace_domain_name> +short
```

#### To perform the trace of the domain name
```
$ dig <replace_domain_name> +short +trace
```
